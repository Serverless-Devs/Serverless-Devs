edition: 1.0.0        #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: component-test   #  项目名称
access: huawei
services:
  component-test: #  服务名称
    component: ../../lib  # 这里引入的是相对路径，正式配置替换成你自己的component名称即可 
    props: 
      projectId: 0bcc05efb100f2a92f53c011f262dfa0
      region: cn-north-4
      function:
        functionName: wzr-fg-test-2021-9-27-1
        handler: index.handler
        memorySize: 128
        timeout: 30
        runtime: Node.js8.10
        package: default
        codeType: zip
        code:
          codeUri: ./
      trigger:
        triggerTypeCode: TIMER
        eventTypeCode: MessageCreated
        status: DISABLED
        eventData:
          name: Timeer-fbb8
          schedule: 3m
          schedule_type: Rate
          user_event: ""