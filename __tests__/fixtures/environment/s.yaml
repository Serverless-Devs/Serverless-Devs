edition: 3.0.0
name: web-framework-app
access: "default"

resources:
  function-a:
    component: v3test
    props: 
      function:
        name: fcv3-function-b-${environment.name}
        codeUri: './code'
        runtime: python3.9
        handler: index.app
        timeout: 60

  function-b:
    component: v3test
    props:
      function:
        name: fcv3-function-b-${environment.name}
        codeUri: './code1'
        runtime: python3.9
        handler: index.app
        timeout: 60