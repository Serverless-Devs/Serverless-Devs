- access: access_testing
  name: testing
  description: 'testing environment'
  type: 'testing' # 环境类别
  region: cn-hangzhou
  role: acs:ram::<account>:role/serverlessdevsinfra-testing
  props:
    function:
      runtime: python3
      handler: index.handler
      codeUri: ./code
      memorySize: 128
      timeout: 60
- access: access_testing
  name: testing
  description: 'testing environment'
  type: 'testing' # 环境类别
  region: cn-hangzhou
  role: acs:ram::<account>:role/serverlessdevsinfra-testing
  props:
    function:
      runtime: python3
      handler: index.handler
      codeUri: ./code
      memorySize: 128
      timeout: 60